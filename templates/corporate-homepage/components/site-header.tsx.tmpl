import Link from "next/link";
import { siteConfig } from "@/lib/site-config";

export function SiteHeader() {
  return (
    <header className="sticky top-0 z-10 border-b border-line bg-background/90 backdrop-blur">
      <div className="mx-auto flex min-h-[68px] max-w-shell flex-col items-start justify-between gap-4 px-6 py-4 md:flex-row md:items-center md:py-0">
        <Link className="font-extrabold" href="/">
          {siteConfig.companyName}
        </Link>
        <nav className="flex flex-wrap items-center gap-4 text-sm text-muted" aria-label="Main navigation">
          {siteConfig.navigation.map((item) => (
            <Link className="transition hover:text-foreground" href={item.href} key={item.href}>
              {item.label}
            </Link>
          ))}
        </nav>
      </div>
    </header>
  );
}
