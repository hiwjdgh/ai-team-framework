import { siteConfig } from "@/lib/site-config";

export default function ContactPage() {
  return (
    <section className="px-6 py-16 md:py-20">
      <div className="mx-auto grid max-w-shell gap-12 md:grid-cols-[0.9fr_1.1fr]">
        <div>
          <p className="mb-4 text-sm font-bold uppercase text-gold">Contact</p>
          <h1 className="mb-6 text-5xl font-extrabold leading-none md:text-7xl">Start a conversation.</h1>
          <p className="text-xl leading-8 text-muted">
            Tell us what you are trying to build, improve, or clarify. We will respond with a
            practical next step.
          </p>
          <p className="mt-6 leading-7 text-muted">
            {siteConfig.email}
            <br />
            {siteConfig.phone}
          </p>
        </div>
        <form className="rounded-lg border border-line bg-panel p-7">
          <div className="grid gap-4">
            <label className="grid gap-2 text-sm font-bold text-muted">
              Name
              <input className="w-full rounded-md border border-line bg-white p-3 text-foreground" name="name" type="text" />
            </label>
            <label className="grid gap-2 text-sm font-bold text-muted">
              Email
              <input className="w-full rounded-md border border-line bg-white p-3 text-foreground" name="email" type="email" />
            </label>
            <label className="grid gap-2 text-sm font-bold text-muted">
              Message
              <textarea className="min-h-32 w-full resize-y rounded-md border border-line bg-white p-3 text-foreground" name="message" />
            </label>
            <button className="inline-flex min-h-12 items-center justify-center rounded-md bg-gold px-5 font-bold text-[#1a1206]" type="submit">
              Send message
            </button>
          </div>
        </form>
      </div>
    </section>
  );
}
